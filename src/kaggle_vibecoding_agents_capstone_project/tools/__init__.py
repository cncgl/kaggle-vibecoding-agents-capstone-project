"""Tools the agents call to reach the real world (weather, places, ...).

MVP: deterministic / curated providers so the loop runs offline with no keys.
Next: replace places with the Maps MCP server and weather with live open-meteo.
"""
