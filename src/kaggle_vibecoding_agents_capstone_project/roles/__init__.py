"""Agent roles (the multi-agent system).

orchestrator → profiler/memory, planner, verifier. Each role is a thin Python
class today; the LLM-backed versions plug in where marked `TODO: ADK/Gemini`.
"""
